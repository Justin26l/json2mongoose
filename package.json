{
  "name": "json2mongoose",
  "version": "0.0.1",
  "description": "genarate mongoose ts files from jsonSchema",
  "author": "justin26l",
  "license": "MIT",
  "main": "index.js",
  "bin": {
    "json2mongoose": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "jsonschema",
    "mongoose"
  ],
  "dependencies": {
    "json-schema-to-typescript": "^13.1.1"
  }
}
